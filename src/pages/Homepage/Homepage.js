import React from 'react'
import Preview from '../components/Preview/Preview'

export default function Homepage() {
    return (
        <div>
            map over the preview components
        </div>
    )
}
